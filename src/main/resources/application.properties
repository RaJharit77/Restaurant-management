spring.application.name=RestaurantManagementApps

# Database Configuration
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:5432/${DB_NAME:restaurant_management}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:raharison#77}
spring.datasource.driver-class-name=org.postgresql.Driver

# Autre configuration
server.port=8080
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true

# Pour activer le rechargement à chaud de Spring DevTools
spring.devtools.restart.enabled=true
